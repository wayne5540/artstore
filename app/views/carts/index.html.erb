<h1>購物清單</h1>
<table class="table">
  <thead>
    <th>商品名稱</th>
    <th>商品規格</th>
    <th>商品售價</th>
    <th></th>
  </thead>
  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.product.name %></td>
        <td><%= item.spec.name %></td>
        <td><%= item.product.price %></td>
        <td><%= link_to("刪除", cart_item_path(item), :method => :delete) %></td>
      </tr>
    <% end %>

    <tr>
      <td></td>
      <td></td> 
      <td></td>
      <th>總金額：<%= @sum %></th>
    </tr>
  </tbody>
</table>

<%= link_to("結賬囉", "#", :class => "btn btn btn-success") %>  
